/* Cambrian Web UI — the store module.
 *
 * Re-exports the projection store (UI-005) and the shell chrome slice
 * (PRD-02). Future slices (chat, plan, memory, audit) live here too.
 */

export * from './projection';
export * from './shell';
